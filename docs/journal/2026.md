# Project Journal — 2026

---

## 2026-06-05 — Session Summary

**Decisions:**

- Built custom ADR skill (MADR format) over installing adr-kit — adr-kit is designed for team-scale enforcement (pre-commit blocking, LLM judge on every commit, pattern/import rules). Our decisions include workflow and process decisions that adr-kit's enforcement engine can't capture. Right-sized for solo projects; revisit adr-kit at 5+ active developers or when compliance requirements emerge.
- Chose MADR format over Nygard minimal for ADRs — MADR adds "Considered Options" and "Decision Drivers" sections, which capture what was rejected and why. This directly prevents re-litigating the same decisions in future sessions.
- Used PreCompact hook (not a Haiku API call) to trigger journal writing — compaction is already the natural "save context" moment; using the current Claude session avoids extra API cost and keeps the journal written by the model with full conversation understanding.
- Consistency-audit 5th question (app identity) placed in skill only, not duplicated in global CLAUDE.md — CLAUDE.md already mandates invoking the skill; adding it to CLAUDE.md directly would duplicate it and create maintenance drift between the two.
- adr-kit deferred rather than discarded — noted in global CLAUDE.md New Project Checklist as something to assess at project start, alongside RFCs, AGENTS.md, and log4brains. Worth adopting at enterprise scale; not needed now.
- Per-year journal files (`docs/journal/YYYY.md`) over per-session files — avoids clutter while keeping entries scannable; a new file each year keeps history manageable.

**Changes:**

- `retro` skill created — sprint retro format (what went right/wrong/improve + action items), saves to `docs/retros/`, feeds pending items into project memory. See `C:\Users\Miko\.claude\skills\retro\SKILL.md`.
- `consistency-audit` skill updated — added CLAUDE.md fast path, non-UI project support (API/CLI branches), and 5th identity question ("does this feature extend what the system *is*?").
- `adr` skill created — MADR format, stores in `docs/adr/ADR-NNN-title.md`, maintains README index, integrates with consistency-audit and retro. See `C:\Users\Miko\.claude\skills\adr\SKILL.md`.
- `journal` skill created — manual session journaling, appends to `docs/journal/YYYY.md`. See `C:\Users\Miko\.claude\skills\journal\SKILL.md`.
- PreCompact hook updated — now detects major sessions (significant file writes) and injects journal-writing instruction into `additionalContext` before Claude resumes.
- Stop + PreCompact hook scripts created (`C:\ClaudeProjects\scripts\`) — previously configured in `settings.json` but scripts were missing; session logging was a no-op until this session.
- Global CLAUDE.md updated — `adr` and `journal` added to Mandatory Skills table; New Project Checklist extended with assessment note for RFCs, AGENTS.md, log4brains.
- Two retros written — see `docs/retros/2026-06-04-context-lost-clear-command.md` and `docs/retros/2026-06-04-rates-tab-v3-consistency-failure.md`.
- ADR-001 created — math.js for Scientific expression evaluation. See `docs/adr/ADR-001-mathjs-for-scientific-expression-evaluation.md`.

**Pending:**

- Proposal 2: extract established patterns from CLAUDE.md into `docs/standards.md`; have CLAUDE.md reference it
- Write V3.1 Rates tab spec before starting any design or implementation
- Run full design process (consistency-audit → platform research → ui-ux-pro-max) before V3.1 design starts
- Add `/clear` warning to project CLAUDE.md

---

## 2026-06-09 — Session Summary

**Decisions:**
- Chose ASCII mockup workflow as global standard — all UI options must be visualized (with ✅ CHOSEN markers and a ## Decisions table) before any design questions are asked; large mockups saved to `docs/superpowers/specs/mockups/`. Why: showing mockups before a consistency audit caused V3 to ship with wrong input model; ASCII-first + decision gate prevents re-opening design after implementation starts.
- Chose P0–P4 single priority axis over severity+priority dual-axis — rationale: solo project scale doesn't benefit from a 2D matrix; single axis reduces cognitive overhead without losing signal. See `docs/adr/ADR-002-bug-logging-process.md`.
- V3.1 rate table: collapsible (hidden by default), inline keyboard search, swap button between display rows. See `docs/superpowers/specs/mockups/2026-06-09-rates-v31-mockup.md`.
- Rate table search is an intentional exception to the numpad-only input rule — keyboard input is defensible for filtering 150+ reference currencies (not calculator input).
- Chose to fully integrate GitHub features (issue templates, PR template, CI workflow, Dependabot, branch protection) to treat this project with professional software practices.
- Made repo public (was private) to unlock branch protection rules — GitHub requires public repo or Pro plan for branch protection on free tier.

**Changes:**
- Created `bug` skill — P0–P4 process, systematic-debugging prerequisite, `gh issue create` template, retro requirement for P0/P1
- Created `github-setup` skill — canonical templates in `skills/github-setup/templates/`, covers bug/feature issue templates, PR template, CI workflow, Dependabot, SECURITY.md, CONTRIBUTING.md, labels, branch protection
- Added GitHub infrastructure to calculator repo: `.github/ISSUE_TEMPLATE/`, `.github/workflows/ci.yml`, `.github/dependabot.yml`, `SECURITY.md`, `CONTRIBUTING.md`
- Added `bug` to CLAUDE.md Mandatory Skills table; added `/clear` warning to Global Conventions; strengthened mockup format rules; added decisions gate
- Wrote V3.1 design spec: `docs/superpowers/specs/2026-06-09-rates-v31-design.md`
- Wrote V3.1 ASCII mockup: `docs/superpowers/specs/mockups/2026-06-09-rates-v31-mockup.md`
- Logged GitHub issue #2 for V3 consistency bug (Rates tab shipped with web-form input model instead of numpad)
- Wrote V3.1 implementation plan: `docs/superpowers/plans/2026-06-09-rates-v31.md`

**Pending:**
- Execute V3.1 implementation plan (8 tasks: hook rewrite TDD → RatesDisplay → RatesNumpad → CurrencyPickerModal → RatesTable → RatesCalculator → CSS → verify)
- Open PR for `feat/rates-v3.1-numpad` (include `Fixes #2`)
- Merge `docs/standards-extraction` branch (has `docs/standards.md`, ADR-002, bug/github-setup skill commits)
- Set branch protection status check names after CI runs at least once

---

## 2026-06-10 — Session Summary (V3.1 implementation)

**Decisions:**
- Replaced raw `setToCurrency` hook export with named `selectTableCurrency` action — preserves the hook's encapsulation model (all mutations go through named actions, not raw state setters)
- Capped fraction digits at 2 in `pressDigit` — matches `result.toFixed(2)` output precision; prevents inputs like `"1.999999"` that don't match the displayed result format
- Swap with zero result resets `amount` to `""` — prevents `"0.00"` leaking into input state and breaking numpad entry flow
- Filtered "other" currency from CurrencyPickerModal — prevents same-currency selection which produces `"—"` result with no recovery path

**Changes:**
- Wrote V3.1 implementation plan: `docs/superpowers/plans/2026-06-09-rates-v31.md`
- Executed all 8 plan tasks via subagent-driven development (77 tests passing):
  - Rewrote `src/hooks/useRates.js` with numpad action model
  - Rewrote `src/test/useRates.test.js` (37 tests, up from ~12)
  - Created `src/components/RatesDisplay.jsx`, `RatesNumpad.jsx`, `CurrencyPickerModal.jsx`
  - Rewrote `src/components/RatesTable.jsx` (collapsible, row-tap sets toCurrency)
  - Rewrote `src/components/RatesCalculator.jsx` (stateless composer)
  - Deleted `src/components/RatesConverter.jsx`
  - Updated `src/styles/index.css` (removed old V3 CSS, added V3.1 sections)
- Applied two post-review fixes (swap zero-result reset + 2-digit fraction cap)
- Applied two final-review fixes (contradictory test name + modal currency filtering)

**Pending:**
- Visual verification: run `npm run dev` and test golden path at http://localhost:5173
- Open PR for `feat/rates-v3.1-numpad` (include `Fixes #2`)
- Merge `docs/standards-extraction` branch
- Set branch protection status check names after CI runs at least once

---

## 2026-06-10 — Session Summary (process and tooling)

**Decisions:**
- Created `multi-model-review` skill as opt-in (not mandatory) — GPT-5 Codex (~85% SWE-bench, different training lineage) is the best reviewer choice; addresses Claude-reviewing-Claude blind spots without adding friction to every project. Opt-in keeps it accessible without mandating cost on all projects.
- Updated bug skill and global CLAUDE.md to include CI failures blocking a merge as qualifying bug triggers — previously only covered post-merge defects; a CI failure was encountered and the process gap was identified. Tightened "Do NOT log" rule to "before CI runs" rather than all pre-merge cases.
- Removed `@rolldown/binding-win32-x64-msvc` from devDependencies — was auto-added by Vite's Windows installer but should never be a direct dependency; Rolldown already declares all platform bindings as `optionalDependencies`. Direct dep caused `EBADPLATFORM` on Linux CI runners.
- Fixed branch protection required check names: `test`/`build` → `Test`/`Build` — GitHub check names are case-sensitive; mismatch was preventing merge even when CI passed.
- Default currencies changed to CAD (from) and PHP (to) per user preference.

**Changes:**
- Created `C:\Users\Miko\.claude\skills\multi-model-review\SKILL.md` and `templates/workflows/pr-review.yml` — opt-in GitHub Actions workflow sending PR diffs to OpenAI API for cross-model review.
- Updated global `C:\Users\Miko\.claude\CLAUDE.md` — added `multi-model-review` to "Also assess at project start" section; added CI failure row to Mandatory Skills table.
- Updated `C:\Users\Miko\.claude\skills\bug\SKILL.md` — CI failure added as qualifying trigger; description and trigger phrases broadened.
- Filed and fixed GitHub issue #3 (P3): picker modal expanded to full viewport — root cause was `position: fixed` on overlay; fixed with `position: absolute` + `position: relative` on `.rates-calculator`.
- Filed and fixed GitHub issue #6 (P1): CI Linux platform failure — root cause was `@rolldown/binding-win32-x64-msvc` as direct devDependency; fixed by removing from `package.json` and regenerating lockfile.
- `src/hooks/useRates.js`: default `fromCurrency` USD→CAD, `toCurrency` EUR→PHP.
- `src/test/useRates.test.js`: added CAD/PHP to mock data; updated all default assertions to match.
- PR #4 (feat/rates-v3.1-numpad) opened and merged. See ADR-002 for bug logging process decision.
- PR #5 (docs/standards-extraction) opened; pending merge.

**Pending:**
- Merge PR #5 (docs/standards-extraction) — rebase on updated main to trigger CI, then merge.
- Sync local main with remote after PR #4 merge (fast-forward error on local only; remote merge succeeded).

---

## 2026-06-10 — Project Complete

**Final state:**
- 3 tabs shipping: Basic, Scientific, Rates (V3.1)
- 77 unit tests passing
- All Dependabot updates merged (actions/checkout v6, actions/setup-node v6, @types/react 19.2.17, prettier 3.8.4, happy-dom 20.10.2)
- No open PRs, no open issues, main is clean

**Process improvements shipped during this project:**
- `consistency-audit` skill — pre-design pattern audit, created after V3 Rates tab shipped with wrong input model
- `bug` skill — P0–P4 structured workflow, expanded to cover CI failures as qualifying triggers
- `adr` skill — MADR-format architecture decision records
- `journal` skill + PreCompact hook — conversation draft written before compaction so journal is written from live context, not summary
- `retro` skill — post-mortem format for P0/P1 bugs
- `github-setup` skill — one-command project infrastructure
- `multi-model-review` skill — opt-in cross-model code review (GPT-5 Codex as reviewer)
- Global CLAUDE.md platform research step generalized — now covers web apps, desktop apps, CLI tools, and developer APIs in addition to iOS/Android mobile

**Decisions:**
- PreCompact hook redesigned to write full conversation draft to `docs/journal/session-draft-{id}.md` before compaction — fixes the fundamental problem of journaling from a summary rather than the live conversation. No API key required; the hook extracts text-only content from the JSONL transcript.
- Platform research step in global CLAUDE.md broadened to be platform-agnostic — previously iOS/Android specific, now branches by project type so the process applies to any future project regardless of target platform.

---
